{
  "name": "expense-budget-visualizer",
  "version": "1.0.0",
  "description": "Client-side expense tracking app with budget visualization",
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "fast-check": "^3.19.0",
    "@vitest/coverage-v8": "^1.6.0",
    "jsdom": "^24.0.0"
  }
}
